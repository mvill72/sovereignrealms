/**
 * Jungian Archetype Components — The Psychological Layer
 *
 * "The meeting of two personalities is like the contact of two chemical substances:
 *  if there is any reaction, both are transformed."
 *
 * These components implement the Jungian archetypes as living UI patterns:
 * - ArchetypeProvider — Context for active archetype state
 * - HeroCall — Onboarding as the Hero's journey
 * - ShadowConfirmation — Confronting destructive actions
 * - WiseOldOnePrompt — Reflection prompts on each post
 */

export { ArchetypeProvider, useArchetype } from './ArchetypeProvider';
export { HeroCall } from './HeroCall';
export { ShadowConfirmation } from './ShadowConfirmation';
export { WiseOldOnePrompt } from './WiseOldOnePrompt';
